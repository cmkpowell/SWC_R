#create a new data frame with three rows and three columns

nf<-data.frame("first name"=c('Dan', 'Fiona', 'Duncan'),"last name"=c('Powell', 'Powell','Powell'),"fav fruit"=c('banana', 'pomegranate', 'strawberry'))
